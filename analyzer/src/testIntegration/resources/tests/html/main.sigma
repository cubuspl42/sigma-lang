%class HtmlElement ^{
    tagName: String,
    children: ^[],
}

%class TextNode ^{
    content: String,
}

%const Node = HtmlElement | TextNode

%const element: Node = HtmlElement.new{
    tagName: "div",
    children: [],
}

%const main = element
