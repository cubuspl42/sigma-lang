%class Cat {
    %constructor of ^{
        name: String,
        hasSharpClaws: Bool,
    }

    %fun pet ^{} => %when (
        %case this.hasSharpClaws => "Bleeding"
        %else => "Purrr"
    )

    %fun meow ^{} => "Meow!"
}
