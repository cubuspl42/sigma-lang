%class Cat {
    %constructor of ^{
        name,
        hasSharpClaws,
    }

    %fun pet ^{} => %when (
        %case this.hasSharpClaws => "Bleeding"
        %else => "Purrr"
    )

    %fun meow ^{} => "Meow!"
}
